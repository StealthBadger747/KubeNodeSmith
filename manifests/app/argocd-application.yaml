apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: KubeNodeSmith
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/StealthBadger747/KubeNodeSmith.git
    targetRevision: main
    path: manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
